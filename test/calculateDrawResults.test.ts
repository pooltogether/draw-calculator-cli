import { BigNumber } from 'ethers';

describe('call Draw Calc lib', () => {
    it('returns sane result', () => {
        const user = {
            address: '0x00e6acb9346c6d4940a167f7b196104fa7d84c85',
            balance: BigNumber.from('1000000'),
        };
    });
});
